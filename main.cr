# This is a comment in Crystal. It's ignored by the Crystal compiler and used for documentation.

# Define a method that takes a name and prints a greeting
def greet(name)
  puts "Hello, #{name}!"
end

# Call the greet method with different names
greet("Alice")
greet("Bob")
